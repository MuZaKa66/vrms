# ============================================================================
# VRMS - Video Recording Management System
# Complete Dependency List
# ============================================================================
#
# IMPORTANT: System packages MUST be installed BEFORE pip packages
#
# ============================================================================
# STEP 1: Install System Packages (apt)
# ============================================================================
# sudo apt update
# sudo apt install -y python3-pyqt5 python3-opencv python3-pyaudio python3-pygame python3-dateutil ffmpeg portaudio19-dev libsdl2-dev
#
# ============================================================================
# STEP 2: Install Python Packages (pip in virtual environment)
# ============================================================================
# cd /opt/vrms1
# source venv/bin/activate
# pip install -r requirements.txt
#
# ============================================================================

# NumPy (required by OpenCV)
numpy<2.0.0

# Image Processing
Pillow==10.4.0

# Voice Recognition (Offline)
vosk==0.3.45

# I2C for RTC Module (Linux only)
smbus2==0.4.3; platform_system == "Linux"

# ============================================================================
# NOTES:
# - PyQt5, OpenCV, pyaudio, pygame, dateutil are installed via apt
# - Virtual environment MUST be activated before pip install
# - On Pi, use system packages for better performance
# ============================================================================
